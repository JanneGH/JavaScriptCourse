// Remember, we're gonna use strict mode in all scripts now!
'use strict';

/// CHALLENGE 1
// gebruiken op 2 aparte arrays? Ja
// de string zo lang als de array lang is?
// is de print een string? ja
// starten en eindigen met ...? ja

// opbreken in stukjes:
// maak een array van data 1
// maak een array van data 2
// waarde 0
// in indexwaarde days
// [1, 2, 3].join(" ... "); // "1 ... 2 ... 3"

/*
const data1 = [17, 21, 23];
const data2 = [12, 5, -5, 0, 4];
const tempString = [];


// voor elke array met temperaturen per dag
// print een string met de temperatuur en na hoe veel dagen de temperatuur zo was

const createTempString = function (arr) {
  let daysAmount = 1;

  for (let i = 0; i < arr.length; i++) {
    tempString.push(arr[i] + 'C in ' + daysAmount + ' days');
    daysAmount++;
  }
  return tempString.join(' ... ');
};

console.log('...' + createTempString(data1) + '...');
console.log('... ' + createTempString(data2) + '...');
*/
